<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="_EXT">
	<!-- 非工具生成的单表的SQL都以下划线开头 -->
	
	<select id="SelectUserByNameAndPwd" parameterType="HashMap" resultType="com.jelly.eoss.model.Users">
		select
		id as "id",
		username as "username",
		password as "password",
		create_datetime as "createDatetime"
		from users m
		where m.username = #{username}
		and m.password = #{password}
	</select>
	
	<select id="Login_QueryTreePathByUserId" parameterType="int" resultType="LinkedHashMap">
		SELECT m.path AS "ids"
		FROM users u
		INNER JOIN users_menu um ON u.ID = um.USERS_ID
		INNER JOIN menu m ON m.id =um.MENU_ID
		WHERE u.id = #{id}
	</select>
</mapper>
